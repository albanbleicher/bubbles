{
  "name": "my-project",
  "scripts": {
    "dev": "parcel src/scripts/**/** src/index.html",
    "build": "parcel build src/index.html",
    "clean": "rm -rf dist/*.*",
    "docker:build": "docker build -t albanbleicher-bubbles .",
    "docker:run": "docker run -p 3000:80 albanbleicher-bubbles"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "@parcel/transformer-glsl": "^2.12.0",
    "@parcel/transformer-image": "^2.12.0",
    "@parcel/transformer-raw": "^2.12.0",
    "@parcel/transformer-sass": "^2.12.0",
    "gsap": "^3.12.5",
    "three": "^0.134.0",
    "tweakpane": "^3.1.10"
  }
}
